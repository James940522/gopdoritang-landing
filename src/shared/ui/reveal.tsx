'use client';

import { motion } from 'framer-motion';

export const RevealArticle = motion.article;
export const RevealDiv = motion.div;
export const RevealFigure = motion.figure;
export const RevealHeader = motion.header;
export const RevealTr = motion.tr;
